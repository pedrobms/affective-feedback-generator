
// Constants formerly here (GEMINI_MODEL_NAME, SYSTEM_INSTRUCTION, USER_PROMPT_TEMPLATE)
// have been moved to the backend, as the frontend no longer directly interacts with the Gemini API.

// You can add frontend-specific constants here if needed in the future.
export {}; // Ensures this is treated as a module if empty
